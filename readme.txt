1. yarn ( установка зависимостей )
2. gulp fonts (используется 1 раз перед стартом проекта, чтобы конвертировать шрифты .ttf в форматы woff,woff2. )
3. gulp ( сборка для разработки )
4. gulp build --prod ( финальная сборка со сжатием файлов )